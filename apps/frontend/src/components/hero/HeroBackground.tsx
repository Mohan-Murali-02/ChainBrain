const HeroBackground = () => {
  return (
    <div className="absolute inset-0 opacity-40 mix-blend-multiply">
      {/* WebGL Canvas will be migrated here */}
    </div>
  );
};

export default HeroBackground;